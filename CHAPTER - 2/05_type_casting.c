#include <stdio.h>

int main()
{
    int a = 20;
    float b = 30.90;

    int c = (int)b; // if you change the data type it called TYPE CASTING.
    printf("%d\n", c);

    float d = (float)a; // if you change the data type it called TYPE CASTING.
    printf("%f\n", d);

    return 0;
}