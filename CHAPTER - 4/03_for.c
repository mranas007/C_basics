#include <stdio.h>

int main()
{

    // Increment → for loop.
    for (int i = 0; i < 10; i++)
    {
        printf("Increment → = %d\n", i);
    }

    // decremenint for loop.
    for (int i = 10; i; i--)
    {
        printf("decremenint = %d\n", i);
    }

    return 0;
}